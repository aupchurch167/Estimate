{
  "name": "quill",
  "version": "0.0.0",
  "private": true,
  "description": "Quill — AI-powered commercial construction estimating tool.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "build:frontend": "npm --workspace frontend run build",
    "build:backend": "npm --workspace backend run build",
    "typecheck": "npm --workspace frontend run typecheck && npm --workspace backend run typecheck",
    "lint": "npm --workspace frontend run lint && npm --workspace backend run lint",
    "lint:fix": "npm --workspace frontend run lint:fix && npm --workspace backend run lint:fix",
    "format": "npm --workspace frontend run format && npm --workspace backend run format",
    "check": "npm run typecheck && npm run lint"
  },
  "engines": {
    "node": ">=20"
  }
}
