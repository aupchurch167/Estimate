# Tell Prisma which binary target to fetch so it doesn't auto-detect at install time.
# debian-openssl-3.0.x covers Railway's default Ubuntu 22+ / Debian 12 containers.
PRISMA_CLI_BINARY_TARGETS=debian-openssl-3.0.x
